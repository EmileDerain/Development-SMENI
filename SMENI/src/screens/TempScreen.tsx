import {
    StyleSheet,
    Text,
    View,
} from 'react-native';


const TempScreen = () => {
    return (
        <View>
            <Text style={styles.text}>Exemple3 Screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({
    text: {
        color: '#000',
    },
});

export default TempScreen;
